//#pragma once

//#include "state.hpp"
//#include <memory>

// namespace gamelib2 {

//// a state machine
// class StateMachine {
// public:
//    // onstruct and destruct
//    StateMachine();
//    virtual ~StateMachine();

//    // make sure the statemachine is initialized with a valid state
//    void init(std::unique_ptr<State> &state);

//    // update the state machine
//    void update(float dt);

//    // change to new state
//    void ChangeState(std::unique_ptr<State> &state);

// protected:
//    /// current state
//    // std::unique_ptr<State> current_state;

//    /// track if state has changed
//    bool state_changed;

//    /// track if we have been initialized
//    bool inited;
//};

//} // namespace gamelib2
